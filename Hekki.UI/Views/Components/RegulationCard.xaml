<UserControl x:Class="Hekki.UI.Views.RegulationCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Hekki.UI.Views"
             mc:Ignorable="d" >

    <Border CornerRadius="30"
            MinHeight="80"
            MaxWidth="300"
            Margin="0,0,0,10" 
            Padding="24"
            Cursor="Hand"
            Style="{StaticResource HoverBorder}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Name}" 
                       FontSize="20" 
                       FontWeight="SemiBold" 
                       Grid.Row="0" 
                       TextTrimming="CharacterEllipsis"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0" Grid.Row="1">
                <TextBlock FontSize="14" Text="{Binding Version, StringFormat='version {0}'}" />
                <TextBlock Text=" • " FontSize="14"/>
                <TextBlock FontSize="14" Text="{Binding CreationDate, StringFormat='{}{0:dd.MM.yyyy}'}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
