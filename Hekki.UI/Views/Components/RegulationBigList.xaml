<UserControl x:Class="Hekki.UI.Views.RegulationBigList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:Hekki.UI.Views"
             mc:Ignorable="d" >
    
    <StackPanel Margin="20">
        <TextBlock Text="Regulations" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <ItemsControl ItemsSource="{Binding Regulations}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.InputBindings>
                            <MouseBinding MouseAction="LeftClick"
                                          Command="{Binding DataContext.NavigateToRaceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          CommandParameter="{Binding Id}" />
                        </Grid.InputBindings>
                        <components:RegulationCard />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Margin="0,15,0,0">
            
            <TextBlock Text="{Binding PaginationService.PagingText}" Foreground="#999999" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Previous" Margin="0,0,10,0" Command="{Binding PrevCommand}"/>
                <Button Content="Next" Command="{Binding NextCommand}"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
