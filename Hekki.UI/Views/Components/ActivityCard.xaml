<UserControl x:Class="Hekki.UI.Views.ActivityCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Hekki.UI.Views"
             mc:Ignorable="d" >
    
    <Border Background="#1E1E1E" 
            CornerRadius="12" 
            Margin="0,0,0,10" 
            Padding="20,15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Title}" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="{Binding Version}" Foreground="#999999" FontSize="12"/>
                    <TextBlock Text=" • " Foreground="#999999" FontSize="12"/>
                    <TextBlock Text="{Binding Date}" Foreground="#999999" FontSize="12"/>
                </StackPanel>
            </StackPanel>

            <TextBlock Grid.Column="1" Text="{Binding Value}" Foreground="White" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
