<UserControl x:Class="Hekki.UI.Views.RecentActivityList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:Hekki.UI.Views"
             mc:Ignorable="d" >
    
    <StackPanel Background="#121212" Margin="20">
        <TextBlock Text="Recent Activity" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <ItemsControl ItemsSource="{Binding Activities}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <components:ActivityCard />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Margin="0,15,0,0">
            <TextBlock Text="Showing 1-4 of 24" Foreground="#999999" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Previous" Margin="0,0,10,0"/>
                <Button Content="Next" />
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
