<UserControl x:Class="Hekki.UI.Views.Race.Controls.TotalTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hekki.UI.Views.Race.Controls"
             xmlns:converters="clr-namespace:Hekki.UI.Converters"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:Hekki.UI.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:ResourceKeyToStringConverter x:Key="ResourceKeyToStringConverter"/>
    </UserControl.Resources>

    <Border Background="White" BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="12">
        <StackPanel Grid.IsSharedSizeScope="True">

            <!-- Header row -->
            <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1" Padding="15">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Static pilot columns header -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition SharedSizeGroup="col_side" Width="Auto"/>
                            <ColumnDefinition SharedSizeGroup="col_photo" Width="Auto"/>
                            <ColumnDefinition SharedSizeGroup="col_name" Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{DynamicResource m_League}" FontWeight="Bold"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="1" Text="{DynamicResource m_Pilot}" FontWeight="Bold"
                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="2" Text="{DynamicResource m_Name}" FontWeight="Bold"
                                   VerticalAlignment="Center" Margin="0,0,16,0"/>
                    </Grid>

                    <!-- Dynamic result column headers -->
                    <ItemsControl ItemsSource="{Binding Columns}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:ColumnViewModel}">
                                <TextBlock Text="{Binding HeaderResourceKey, Converter={StaticResource ResourceKeyToStringConverter}}"
                                           FontWeight="Bold" TextAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Width" Value="{DynamicResource TotalTable.HeatColumnWidth}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Type}" Value="{x:Static vm:ColumnType.TotalTime}">
                                                    <Setter Property="Width" Value="{DynamicResource TotalTable.TotalColumnWidth}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Type}" Value="{x:Static vm:ColumnType.TotalScore}">
                                                    <Setter Property="Width" Value="{DynamicResource TotalTable.TotalColumnWidth}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
            </Border>

            <!-- Data rows -->
            <ItemsControl ItemsSource="{Binding TotalTableRows}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:TotalTableRowViewModel}">
                        <local:TotalTableRow/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </Border>
</UserControl>
