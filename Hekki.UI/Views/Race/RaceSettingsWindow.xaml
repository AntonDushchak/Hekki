<Window x:Class="Hekki.UI.Views.RaceSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:vm="clr-namespace:Hekki.UI.ViewModels"
        mc:Ignorable="d" 
        Title="{DynamicResource m_RaceSettings}" 
        Height="Auto" 
        Width="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource Brush.Base}">

    <Border Background="{StaticResource Brush.Base}" 
            BorderBrush="{StaticResource Brush.Border}" 
            BorderThickness="1"
            Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="{DynamicResource m_RaceSettings}"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource Brush.TextPrimary}"
                       Margin="0,0,0,20"/>

            <!-- Race Name -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <TextBlock Text="{DynamicResource m_RaceName}"
                           FontSize="13"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           Margin="0,0,0,5"/>
                <TextBox Text="{Binding RaceName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                         MinHeight="30"/>
            </StackPanel>

            <!-- Race Date -->
            <StackPanel Grid.Row="2" Margin="0,0,0,15">
                <TextBlock Text="{DynamicResource m_RaceDate}"
                           FontSize="13"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           Margin="0,0,0,5"/>
                <DatePicker SelectedDate="{Binding RaceDate, Mode=TwoWay}"
                            MinHeight="30"/>
            </StackPanel>

            <!-- Location -->
            <StackPanel Grid.Row="3" Margin="0,0,0,20">
                <TextBlock Text="{DynamicResource m_Location}"
                           FontSize="13"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           Margin="0,0,0,5"/>
                <ComboBox ItemsSource="{Binding AvailableLocations}"
                          SelectedItem="{Binding SelectedLocation, Mode=TwoWay}"
                          MinHeight="30">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="4" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Content="{DynamicResource m_Cancel}"
                        Command="{Binding CancelCommand}"
                        MinWidth="80"
                        Margin="0,0,10,0"/>
                <Button Content="{DynamicResource m_Save}"
                        Command="{Binding SaveCommand}"
                        MinWidth="80"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
